{
  "_comment": "Never published: CopyToPublishDirectory=Never in the csproj, because the bootstrap password below lives in the repository.",
  "DataDir": "./data-dev",
  "Bootstrap": {
    "AdminUser": "admin",
    "AdminPassword": "Compendio!Dev1",
    "AdminEmail": "admin@localhost"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}

<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>Compendio</RootNamespace>
    <AssemblyName>compendio</AssemblyName>
    <UserSecretsId>compendio-server</UserSecretsId>
    <!--
      A culture missing from this list is silently dropped from the build output, which is how a
      Spanish instance starts answering in English. Adding a locale means adding it here too.
    -->
    <SatelliteResourceLanguages>en;es</SatelliteResourceLanguages>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Identity.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.Extensions.Hosting.Systemd" />
    <PackageReference Include="Microsoft.Extensions.Hosting.WindowsServices" />
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" />
    <PackageReference Include="System.Security.Cryptography.ProtectedData" />
    <PackageReference Include="Markdig" />
    <PackageReference Include="YamlDotNet" />
    <PackageReference Include="HtmlSanitizer" />
    <PackageReference Include="DiffPlex" />
    <PackageReference Include="Common.Mediator" />
  </ItemGroup>

  <!--
    The built-in user guide. Embedded rather than copied to disk so that a single-file deployment
    still has its documentation — a help button that 404s because somebody copied only the
    executable is worse than no help button. A new language is a new folder here and nothing else.
  -->
  <ItemGroup>
    <EmbeddedResource Include="Resources\Help\**\*.md" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Compendio.Server.Tests" />
  </ItemGroup>

  <!-- appsettings.Development.json carries a bootstrap password that lives in the repository. -->
  <ItemGroup>
    <Content Update="appsettings.Development.json">
      <CopyToPublishDirectory>Never</CopyToPublishDirectory>
    </Content>
  </ItemGroup>

  <Import Project="Client.targets" />

</Project>
